<Window x:Class="MiniProjekat.TableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiniProjekat"
        mc:Ignorable="d"
        Title="US Economic Indicators - Table"
        Icon="./icons/logo.png"
        Height="650" 
        Width="400"
        FontFamily="Roboto Mono"
        FontSize="15">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>

        <DataGrid x:Name="TableXAML" HorizontalAlignment="Left" Margin="20,20,20,20" Width="auto" Grid.ColumnSpan="1" Grid.Column="0" Grid.Row="0" IsReadOnly="True">

            <DataGrid.Resources>
                <Style TargetType="{x:Type DataGridColumnHeader}">
                </Style>
            </DataGrid.Resources>


            <!-- Column Header & Text bindings-->
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding date}" Width="*"/>
                <DataGridTextColumn Header="Value" Binding="{Binding value}" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</Window>
